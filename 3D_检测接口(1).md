## 深度学习鲁棒**性评**估

------

### 3D图像检测

#### 接口一：`prepare_dataset` — 数据集准备

**接口说明：**检验数据集目录结构是否符合要求，完成数据集初始化。

**文件名称：**`dataset_service.py`

**数据集目录要求：**

平台通过目录内是否存在 `dataset.yaml` 自动判断类型：

**KITTI 格式**

```
<dataset_path>/
├── training/
│   ├── velodyne/    ← 点云文件，每帧一个 .bin
│   ├── calib/       ← 标定参数
│   └── label_2/     ← GT 标注
└── ImageSets/
    └── val.txt      ← 帧列表（缺失时自动生成）
```

**Custom 格式**（自定义）

```
<dataset_path>/
├── dataset.yaml     ← 必须（DATASET=CustomDataset）
├── points/          ← 点云文件，每帧一个 .npy（形状 N×4）
├── labels/          ← 标注文件，每行: x y z dx dy dz heading category
└── ImageSets/
    └── val.txt      ← 帧列表（缺失时自动生成）
```

**输入参数：**

- `dataset_path`:类型为`str`，表示数据集的路径

**输出：**

```python
bool   # 成功返回 `True`，否则 `False`。
```

------

#### 接口二：`validate_model` — 模型校验

**接口说明：**检验模型和数据集。

**文件名称：**`model_service.py`

**模型目录要求：**

调用时传入**模型目录**，平台按如下规则自动识别文件：

```
model_dir/
├── model.pth          ← 权重（有且仅有一个 .pth 或 .pt）
├── model.yaml         ← 配置（可选，无则使用内置默认配置）
└── my_interface.py    ← ModelInterface 实现（可选，无则使用平台内置实现）
```

**输入参数：**

- `model_path`:类型为`str`，表示模型的路径
- `dataset_path`:类型为`str`，表示数据集的路径

**输出：**

```python
bool   # 成功返回 `True`，否则 `False`。
```

------

#### 接口三：`generate_adversarial` — 对抗样本生成

**接口说明：**生成对抗样本，并返回相似度指标。

**文件名称：**`attack_service.py`

**输入参数：**

- `model_path`:类型为`str`，表示模型的路径
- `dataset_path`:类型为`str`，表示数据集的路径
- ``adv_dataset_path``:类型为`str`，表示对抗样本的生成路径
- ```attack_method```:类型为`str`，表示具体的攻击方法

**输出参数：**

- `result_dict`：类型为 `dict`，包含多个指标的测试结果，表示对抗样本和干净样本的相似度指标。
- `adv_dataset_path`中保存生成的对抗样本。

```python
result_dict = {
    "mean_L2":mean_L2,     # 平均扰动距离
    "max_L2":max_L2,       # 最大扰动距离
    "mad":mad,             # 平均失真度
    "chamfer":chamfer,     # 倒角距离
    "pts_ratio":pts_ratio  #点数变化比例
}
#3D检测的鲁棒性指标，其他图像类不一定是这些。
```

------

#### 接口四：`evaluate_robustness` — 鲁棒性评测

**接口说明：**进行鲁棒性评测。

**文件名称：**`eval_service.py`

**输入参数：**

- `model_path`:类型为`str`，表示模型的路径
- `dataset_path`:类型为`str`，表示数据集的路径
- ``adv_dataset_path``:类型为`str`，表示对抗样本的路径

**输出参数：**

- `result_dict`：类型为 `dict`，包含多个指标的测试结果，里面分为干净样本的测试结果和对抗样本的测试结果

```python
result_dict = {
    # 原始干净样本的检测结果
    "clean": {
        "mAP_3D": 64.2471  # 3D检测平均精度(中等难度, 40召回点)
    },
    # 对抗攻击后样本的检测结果
    "Adversarial": {
        "mAP_3D": 21.8760,   # 对抗样本3D检测平均精度(中等难度, 40召回点)
        "mAP_drop": 42.3711,               # 检测精度下降值
        "mAP_retention_rate": 0.3405,      # 精度保留率
        "attack_success_rate": 0.6595,      # 对抗攻击成功率
    }
}
#3D检测的有效性指标，其他图像类不一定是这些。
```

