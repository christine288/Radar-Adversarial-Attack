# 雷达航迹分类鲁棒性评测接口文档

## 任务说明

本仓库提供**雷达航迹多分类**任务的鲁棒性评测接口。外部项目只需调用以下四个文件中的函数：

- `dataset_service.py`
- `model_service.py`
- `attack_service.py`
- `eval_service.py`

---

## 1. 数据集准备

接口文件：`dataset_service.py`

相对地址：`dataset_service.py`

```python
def prepare_dataset(dataset_path: str) -> bool:
    ...
```

### 输入参数

- `dataset_path`：数据集根目录。

### 数据集目录要求（MAT 航迹分类式）

```text
<dataset_path>/
|-- <class_a>/          # 类别名即标签，如 UAV、bird、speedboat
|   |-- sample_001.mat
|   `-- ...
|-- <class_b>/
|   `-- ...
`-- ...
```

要求：

- 根目录下至少有一个**类别子目录**；
- 每个类别子目录内至少有一个 `*.mat` 文件；
- `.mat` 后续加载时需为 HDF5（v7.3）格式，并含 12 通道航迹字段（见下文「航迹字段」）；`prepare_dataset` 阶段只检查目录结构和文件存在性。

### 返回值

```python
True   # 数据集结构检查通过
False  # 路径不存在或结构不符合要求
```


## 2. 模型校验

接口文件：`model_service.py`

相对地址：`model_service.py`

```python
def validate_model(model_path: str, dataset_path: str) -> bool:
    ...
```

### 输入参数

- `model_path`：模型目录。
- `dataset_path`：数据集根目录

### 模型路径要求

**传模型目录**

```text
<model_path>/
|-- xxx.pth 或 xxx.pt   # 必须：目录内有且仅有一个 .pth 或 .pt 文件，文件名不限
|-- model.yaml          # 可选：模型配置
`-- my_interface.py     # 可选：存在时必须定义 ModelInterface
```

要求：

- `model_path` 必须是目录，不能直接传 `.pth` / `.pt` 文件；
- 目录内必须有且仅有一个 `.pth` 或 `.pt` 权重文件；
- 目前接口仅支持与 `RadarTrackTransformer` 结构兼容的 PyTorch 权重；用户上传的模型可以不是本仓库训练出的文件，但其参数名和张量形状必须能加载到 `RadarTrackTransformer`；
- 推荐 checkpoint 结构包含 `input_size`、`model_state_dict`、`num_classes`；若上传的是纯 `state_dict`，接口会尝试从参数形状推断部分配置，并结合默认配置或 `model.yaml` 加载；
- `model.yaml` 可选；若提供，接口会读取其中的扁平配置项，如 `input_size`、`num_classes`、`model_type`、`d_model`、`nhead`、`num_layers`、`dim_feedforward` 等；
- `my_interface.py` 可选；若提供，接口会检查其中是否定义 `ModelInterface`。

### 支持的权重文件格式

**1. 推荐格式：checkpoint 字典**

```python
{
    "input_size": (32, 12),              # 输入形状：(T, C)，C 必须为 12
    "num_classes": 6,                   # 类别数
    "model_state_dict": model.state_dict()
}
```

这种格式信息最完整，接口会直接读取 `input_size`、`num_classes` 和 `model_state_dict` 来构建并加载 `RadarTrackTransformer`。

**2. 兼容格式：纯 state_dict**

```python
model.state_dict()
```

这种格式只保存模型参数。接口会尝试从参数名和张量形状推断模型配置；若无法完全推断，可在同一模型目录下提供 `model.yaml` 补充配置。

示例 `model.yaml`：

```yaml
input_size: [32, 12]
num_classes: 6
model_type: transformer
d_model: 128
nhead: 8
num_layers: 4
dim_feedforward: 256
```

注意：以上两种格式都要求权重参数与 `RadarTrackTransformer` 结构兼容；如果是 CNN、LSTM 或其他自定义网络结构，即使文件后缀是 `.pth` / `.pt`，当前接口也不支持。

### 返回值

```python
True   # 模型与数据集校验通过
False  # 校验失败
```

## 3. 对抗样本生成

接口文件：`attack_service.py`

相对地址：`attack_service.py`

```python
def generate_adversarial(
    model_path: str,
    dataset_path: str,
    adv_dataset_path: str,
    attack_method: str,
) -> dict:
    ...
```

### 输入参数

| 参数 | 说明 |
|------|------|
| `model_path` | 模型目录，目录内有且仅有一个 `.pth` 或 `.pt` 文件 |
| `dataset_path` | 测试集根目录，格式同接口一 |
| `adv_dataset_path` | 对抗样本输出目录；若目录已存在，接口会先删除再重新生成 |
| `attack_method` | 攻击方法名（见下表） |

### 支持的 `attack_method`

| 类型 | 取值 |
|------|------|
| 白盒 | `fgsm`, `pgd`, `cw` |
| 黑盒 | `square`, `nes`, `transfer` |
| 自然噪声 | `gaussian`, `salt_pepper`, `speckle` |

### 输出

- 对抗数据写入 `adv_dataset_path`，**目录结构与源 MAT 数据集一致**（按类别子目录 + 同名 `.mat`）。
- 返回雷达航迹扰动统计。统计对象为对抗样本与原始样本在**逆预处理后的 12 通道航迹空间**中的逐时刻差异：

```python
result_dict = {
    "mean_L2": mean_L2,             # 所有航迹时刻的平均 L2 扰动距离，按 12 通道差值计算
    "max_L2": max_L2,               # 所有航迹时刻中的最大 L2 扰动距离
    "mad": mad,                     # L2 扰动距离相对 mean_L2 的平均绝对偏差，反映扰动波动程度
    "mean_abs_delta": mean_abs_delta,   # 所有通道、所有时刻的平均绝对扰动
    "mean_rel_change": mean_rel_change, # 平均相对扰动，分母为 max(|原值|, 0.01)
    "max_rel_change": max_rel_change,   # 最大相对扰动，分母为 max(|原值|, 0.01)
}
```


## 4. 鲁棒性评测

接口文件：`eval_service.py`

相对地址：`eval_service.py`

```python
def evaluate_robustness(
    model_path: str,
    dataset_path: str,
    adv_dataset_path: str,
) -> dict:
    ...
```

### 输入参数

- `model_path`：同接口二。
- `dataset_path`：测试集。
- `adv_dataset_path`：接口三生成的对抗集。

**注意：**须先完成接口三，且 `adv_dataset_path` 目录必须已存在。

### 输出 `result_dict`

```python
result_dict = {
    "clean": {
        "accuracy": clean_accuracy,                     # 干净集总体准确率（0~1）
        "macro_f1": clean_macro_f1,                     # 干净集宏平均 F1（0~1）
        "classification_mAP": clean_classification_mAP, # 干净集 one-vs-rest 平均精度（0~1）
    },
    "Adversarial": {
        "accuracy": adv_accuracy,                         # 对抗集总体准确率（0~1）
        "macro_f1": adv_macro_f1,                         # 对抗集宏平均 F1（0~1）
        "classification_mAP": adv_classification_mAP,     # 对抗集 one-vs-rest 平均精度（0~1）
        "mAP_retention_rate": mAP_retention_rate,         # adv classification_mAP / clean classification_mAP
        "attack_success_rate": attack_success_rate,       # 干净预测正确样本中，对抗后预测变错的比例
        "robust_accuracy": adv_accuracy,                  # 鲁棒准确率，等同于对抗集 accuracy
    },
}
```

---

## 5. 航迹字段与模型

单条航迹输入模型前会被重采样为固定长度 `T`，通道数 `C=12`。接口默认使用 `T=32`；如模型目录中提供了与权重同名的 `.meta.json`，接口会优先读取其中的重采样长度配置。

```text
V_m, R_m, A_m, V, R, A, E_m, E, DATA_time, GPS_time_in_data, Iframecnt, SNR
```

模型：`RadarTrackTransformer`，输入形状 `(B, T, C)`。外部调用方只需保证上传的模型权重与该 12 通道航迹输入约定一致。

---

## 6. 调用示例

外部项目调用前，需要保证当前目录或 Python 路径可以导入本仓库根目录下的接口文件。

```python
from dataset_service import prepare_dataset
from model_service import validate_model
from attack_service import generate_adversarial
from eval_service import evaluate_robustness

model_path = r"dataoutput/model_dir"
dataset_path = r"project_data/test"
adv_dataset_path = r"project_data/test_adv_square"
attack_method = "square"

assert prepare_dataset(dataset_path)
assert validate_model(model_path, dataset_path)

attack_result = generate_adversarial(
    model_path=model_path,
    dataset_path=dataset_path,
    adv_dataset_path=adv_dataset_path,
    attack_method=attack_method,
)

eval_result = evaluate_robustness(
    model_path=model_path,
    dataset_path=dataset_path,
    adv_dataset_path=adv_dataset_path,
)

print("接口三 attack_result =", attack_result)
print("接口四 eval_result =", eval_result)
```

## 7. 注意事项

- 外部调用前需要保证当前目录或 Python 路径可以导入 `dataset_service.py`、`model_service.py`、`attack_service.py`、`eval_service.py`。
- `model_path` 必须传模型目录，目录内有且仅有一个 `.pth` 或 `.pt` 权重文件。
- `dataset_path` 只接受 `<dataset_path>/<class_name>/*.mat` 格式的数据集。
- `attack_method` 不区分大小写，建议统一传入小写；当前仅支持 `fgsm`、`pgd`、`cw`、`square`、`nes`、`transfer`、`gaussian`、`salt_pepper`、`speckle`，不支持 `whitebox_pgd`、`natural_gaussian` 等旧别名。
